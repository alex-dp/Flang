<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.depa.flang">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.BaseActivity"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.SharableActivity"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.Settings"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.Progress"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.WordInfo"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.Test"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.TestHistory"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.Help"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.TestResults"
            android:configChanges="orientation|screenLayout|screenSize" />
        <activity
            android:name=".ui.activities.TestableActivity"
            android:configChanges="orientation|screenLayout|screenSize" />

        <service android:name=".NotificationService" /><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
